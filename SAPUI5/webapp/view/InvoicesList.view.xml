<mvc:View controllerName="jeperez.SAPUI5.controller.InvoicesList"
          xmlns="sap.m" 
          xmlns:mvc="sap.ui.core.mvc">
          <List id="invoiceListId"
                headerText="{i18n>invoiceListTitle}" 
                class="sapUiResponsiveMargin" 
                width="auto" 
                items="{ path: 'modelInvoices>/Invoices',
                         sorter: { path : 'ShipperName',
                                   group: true }
                       }">
            <headerToolbar>
                <Toolbar >
                    <Title text="{i18n>invoiceListTitle}"/>
                    <ToolbarSpacer />
                    <SearchField width="50%" search=".onFilterInvoices"/>
                </Toolbar>
            </headerToolbar>
            <items>
                <ObjectListItem title="{modelInvoices>Quantity} x {modelInvoices>ProductName}"
                                number="{ parts : [{path: 'modelInvoices>ExtendedPrice'},
                                                   {path: 'currency>/eur'}],
                                          type: 'sap.ui.model.type.Currency',
                                          formatOptions: {
                                              showMeasure : false
                                          }  
                                        }"
                                numberUnit="{currency>/eur}"
                                numberState="{= ${modelInvoices>ExtendedPrice} > 50 ? 'Success' : 'Error'}" >
                    <firstStatus>
                        <ObjectStatus text="{ path: 'modelInvoices>Status',
                                              formatter: '.formato.invoiceStatus'}"></ObjectStatus>
                    </firstStatus>
                </ObjectListItem>
            </items>
          </List>
</mvc:View>